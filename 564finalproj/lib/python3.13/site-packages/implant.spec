# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['implant.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=['_frozen_importlib', 'requests'],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
a.datas += Tree('./requests', prefix='requests')
a.datas += Tree('./certifi', prefix='certifi')
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='implant',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
